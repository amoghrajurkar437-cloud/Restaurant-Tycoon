package main.tile;
import java.awt.image.BufferedImage;

public class Tile {
    public BufferedImage image; // Image representing the tile, which can be used for rendering the tile on the screen
    public boolean collision = false; // Indicates whether the tile has collision properties, which can be used to determine if the player can pass through the
}